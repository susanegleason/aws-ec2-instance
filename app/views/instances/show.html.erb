<div>
  <h3>E2 Instance: <%= @id %></h3>

  <% if @details %>
    <table>
      <tbody>
        <% @details.each do |key, value| %>
        <tr >
          <td><%= key %></td>
          <td><%= value %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <br><%= link_to "Refresh", instance_url(@id) %>
    <br><%= link_to "Start Instance", instance_start_url(@id) %>
    <br><%= link_to "Stop Instance", instance_stop_url(@id) %>
  <% end %>

</div>
